import {test} from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import ts from 'typescript';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const compiled=ts.transpileModule(fs.readFileSync(new URL('../lib/engagement/validation.ts',import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;const module={exports:{}};new Function('require','module','exports',compiled)(require,module,module.exports);const {evidenceSchema,gymSchema,cosmeticSchema,sessionSchema}=module.exports;const id='11111111-1111-4111-8111-111111111111';
test('Step 4 boundary validation rejects timestamps, rewards, invalid coordinates and forged inventory',()=>{assert.equal(evidenceSchema.safeParse({session:id,request:id,kind:'heartbeat',data:{visible:true}}).success,true);for(const data of [{confidence:100},{xp:999},{status:'passed'},{active_seconds:99999},{timestamp:'yesterday'},{latitude:91},{longitude:Infinity},{accuracy:-1},{challenge:'invalid'},{reflection:'short'}])assert.equal(evidenceSchema.safeParse({session:id,request:id,kind:'location',data}).success,false);assert.equal(gymSchema.safeParse({name:'Gym',latitude:19,longitude:72}).success,true);assert.equal(gymSchema.safeParse({name:'Gym',latitude:19,longitude:72,radius:99999}).success,false);assert.equal(cosmeticSchema.safeParse({item:'frames-1',action:'buy',price:0}).success,false);assert.equal(sessionSchema.safeParse({quest:id,gym:null,strong:false,owner:id}).success,false);});
